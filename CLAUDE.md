# Languages & Success (L&S) — contexte projet

Site marketing + **espace documents** pour un organisme de formation en langues (Nice, certifié Qualiopi). FR. Itérations rapides, style éditorial chaud (rose-gold).

## Stack
- Front : **HTML/CSS/JS statique, sans framework**. CSS partagé `assets/site.css`. Nav/footer injectés par `assets/partials.js`. **Polices auto-hébergées** (`assets/fonts.css` + `assets/fonts/`) : aucune requête vers Google, cf. section Conformité.
- Back : **Node + Express** (`server.js`) qui sert le site **et** une API REST. Base = fichier **`data/db.json`** + fichiers dans **`data/uploads/`**. Auth bcrypt + JWT (token en `localStorage` côté client).
- **Robustesse DB** (`loadDB`/`save`/`backupDB`) : mots de passe **hachés bcrypt** (jamais en clair) ; **écriture atomique** (`.tmp` + `fs.renameSync`, pas de fichier à moitié écrit si crash) ; **sauvegardes horodatées rotatives** dans `data/backups/` (snapshot au démarrage + toutes les 6 h, 60 max ≈ 2 semaines) ; **auto-restauration** depuis le backup valide le plus récent si `db.json` est corrompu/illisible (sinon base neuve). `save()` appelé à chaque mutation = sauvegarde permanente. (Migration SQLite = piste future si besoin de concurrence/chiffrement au repos.)
- Anim héros : `morph.js` (canvas 2D, nuage de particules + logo). `medallion.js`/`sphere.js`/`worldmap.js`/`painting.js` = en réserve, non chargés.
- **i18n (langue du site)** : sélecteur dans le header (pastille `FR ▾` desktop, grille dans le drawer mobile), langues FR (source) / EN / ES / IT / RU / ZH. Moteur `assets/i18n.js` (injecté par `partials.js` comme account.js) : dictionnaires « texte FR normalisé → traduction » appliqués aux nœuds texte + attributs (placeholder/title/aria-label/alt), MutationObserver pour le DOM injecté, persistance `localStorage 'ls-lang'`, `<html lang>` mis à jour, retour FR = restauration des originaux. **TOUT le site vitrine est traduit** : dictionnaires par langue dans `assets/i18n-{en,es,it,ru,zh}.js` (~300 clés chacun, chargés À LA DEMANDE par le moteur — un visiteur FR ne charge rien), couvrant chrome + index + formations (+ modal certifications de certs.js) + financement + entreprises + à-propos + contact + test (UI/niveaux) + blog + page espace-documents. L'**app espace-documents est traduite aussi** (~140 clés : auth, dashboard, dossiers/chat/canaux, notifications, picker + modales de génération, statuts QS/présence, dialogues) — les libellés composites (« Dossier — {nom} », « Séance N », « Bonjour {prénom} », textes de notifs stockés en base, titres numérotés du picker) restent FR par construction. Restent en FR : pages légales (CGV/mentions/confidentialité/RI, volontaire — la version française fait foi), les **documents générés** (serveur, toujours français), les explications de correction du test. ⚠️ selects du test : `value="Anglais"` etc. explicites (le texte affiché se traduit, la value FR pilote la banque de questions) ; formations.html capture le nom FR des langues avant traduction pour le lien `?lang=`. ⚠️ Les zones sous `data-i18n-skip` ne sont JAMAIS traduites — posé sur `#q-text`, `#opts`, `#res-correction` du test de niveau pour que les questions restent dans la langue TESTÉE quel que soit le changement de langue du site (même en plein test).

## Lancer / arrêter
- **Port** : `process.env.PORT || process.argv[2] || 3000`. **Défaut = 3000** (aligné avec la prod) ; en **local on lance toujours sur 8000** : `node server.js 8000` → http://localhost:8000. (Sert site + API.) La preview MCP utilise 8011.
- Le port peut être passé en argument : `node server.js 8011`.
- Données sensibles bloquées par le serveur (`/data`, `node_modules`, `server.js`).
- ⚠️ Toujours **un seul** serveur Node sur un port donné (sinon il écrase `db.json`). Tuer le port avant de relancer.

## Espace documents (`espace-documents.html` + `assets/account.js` + API)
- Comptes + rôles : `admin` / `eleve` / `prof`. **Libellés** : Administrateur / **Apprenant** / **Formateur**.
- **Inscription publique FERMÉE** (24/07/2026) : `POST /api/signup` renvoie 403, la page de connexion n'a plus d'onglet « Créer un compte » (ni le drawer mobile). Les comptes sont créés **par l'admin** : bouton « + Créer un compte » dans la vue globale → modale (mêmes champs/fiches que l'ancienne inscription, rôles apprenant/formateur uniquement, **sans champ mot de passe**) → `POST /api/admin/users` (admin only).
- **Le mot de passe est choisi par la personne elle-même, à la première connexion** (27/07/2026) : l'admin ne saisit ni ne transmet jamais de mot de passe. À la création, le compte reçoit un `passwordHash` inutilisable (aléatoire) + `activation {token, exp}` (32 octets aléatoires, **14 jours**, **usage unique**) + `mustActivate:true`, et un e-mail « Votre compte espace documents est prêt » avec le lien `SITE_URL/espace-documents.html#activation=<token>` (`newActivation`/`sendActivationMail`). Côté client, `boot()` détecte le hash → `renderActivate()` : `GET /api/activate/:token` (vérifie le lien, renvoie prénom+identifiant, 404 si invalide/expiré → écran « Lien expiré » + retour connexion) puis `POST /api/activate {token,password}` (**6 caractères mini**, pose le hash, supprime `activation`+`mustActivate`, enregistre l'entrée dans `db.logins`, renvoie un JWT = **connexion automatique**) ; le jeton est retiré de l'URL (`history.replaceState`) et la session en cours est fermée quand le lien est valide (poste partagé). `POST /api/login` répond **403 avec un message explicite** tant que `mustActivate` (message distinct si le lien a expiré) au lieu de « mot de passe incorrect ». **Renvoi du lien** : bouton ✉️ par compte dans la vue globale → `POST /api/admin/users/:id/reinvite` (admin only, refusé sur un compte admin) — régénère le jeton **sans** remettre `mustActivate`, donc quelqu'un qui a déjà son mot de passe n'est jamais bloqué. `/api/admin/overview` expose `pending` (= `mustActivate`, badge ⏳ « En attente ») et **jamais le jeton**. **Compte admin permanent seedé** : `admin@languagesandsuccess.com`. ⚠️ **AUCUN mot de passe réel n'est écrit dans le dépôt** (il est public, 05/08/2026) : `ADMIN_MDP_INITIAL` vaut `process.env.ADMIN_PASSWORD` ou le bouchon `changez-ce-mot-de-passe`, et cette valeur **ne sert qu'à la CRÉATION** du compte. `ensureDemo` ne réécrase jamais un compte existant : sur une base déjà peuplée (production comprise) **changer la constante ne change RIEN**. Le vrai mot de passe se pose avec **« Changer mon mot de passe »** dans l'espace documents. Les outils `blog/outils/creer.js` et `sync-prod.js` lisent `LS_MDP_PROD` et **s'arrêtent avec un message** si elle est absente, plutôt que d'essayer un mot de passe deviné (`$env:LS_MDP_PROD = '…'` sous PowerShell).
- **Historique de connexions** (`db.logins`, borné aux 1000 dernières) : chaque login réussi enregistre `{user, email, ip, date}` — IP via `cf-connecting-ip` (Cloudflare) → `x-forwarded-for` → remoteAddress (helper `clientIp`). `GET /api/admin/logins` (admin only, `?user=<id>` pour filtrer, 200 dernières, nom résolu ou « (compte supprimé) »). UI : bouton « 🕐 Historique de connexions » (global) dans la barre admin + bouton 🕐 par compte dans la liste (modale `openLoginsModal`).
- **Modèle DOSSIER : UN apprenant, PLUSIEURS formateurs** (27/07/2026 — avant : exactement 1 formateur) : `db.groups` = `{id, profs:[userId], eleve:userId, date}`. Un dossier suit **un** apprenant (c'est SON dossier) mais peut réunir **autant de formateurs que voulu**. **L'admin est membre de TOUS les dossiers** (entité virtuelle `'admins'`, affichée « Administration L&S » — le nom de l'admin réel n'est jamais montré aux non-admins). **Migration automatique au chargement** (`migrateGroups` appelé par `normalizeDB`) : `g.prof` → `g.profs:[…]`, `g.eleve` conservé tel quel, ancien champ `prof` supprimé.
  - **Accesseurs** : `gProfs(g)` (liste) / `gMembers(g)` / `gProfUsers(g)` ; l'apprenant se lit directement `g.eleve`. ⚠️ ne jamais relire `g.prof`.
  - **Création : ADMIN UNIQUEMENT** (30/07/2026 — avant, un formateur pouvait s'ajouter un apprenant). `POST /api/groups` répond **403 à tout non-admin** ; l'admin envoie `{profIds:[], eleveId}` (au moins un formateur, dédoublonnés et **triés alphabétiquement** pour un ordre stable). Côté client, le bouton loupe « 🔍 Ajouter un apprenant » et toute la modale de recherche (`openAddModal`/`ADD_CANDIDATES`/`roleAllowed`, CSS `.add-*`) sont **supprimés** ; `renderUserDash` n'appelle plus `/api/users`. La composition d'un dossier est donc entièrement une décision de l'administration.
  - **Composition modifiable À TOUT MOMENT** (`PATCH /api/groups/:id`, admin only) : **la liste des formateurs uniquement** — l'apprenant d'un dossier ne change pas. Un formateur retiré perd l'accès ; documents et messages déjà déposés restent. **Deux entrées** (30/07/2026) : bouton **« 👥 Formateurs »** dans l'en-tête du dossier ouvert (`.grp-edit-btn`, admin seul, à côté de « Générer un document » ; les deux sont groupés dans `.ds-card-acts` pour s'empiler proprement à 375 px) **et** ✏️ sur chaque dossier de la vue globale → `openEditGroup` (cases à cocher `.pick-list`). **Rien n'est appliqué sans `confirmDialog`** nommant les personnes concernées (« X ne verra plus ce dossier… » / « Y aura accès… y compris à tout l'historique du canal privé déjà échangé ») ; aucun changement = fermeture silencieuse sans confirmation ; décocher le dernier formateur = refus (« Un dossier doit garder au moins un formateur. »). ⚠️ `openEditGroup` doit tolérer les **deux formes** de dossier : `{id,name}` (vue globale) et la fiche complète `{prenom,nom}` (`/api/groups`).
  - **Suppression d'un compte** : supprimer l'**apprenant** supprime son dossier (cascade) ; supprimer un **formateur** le retire seulement de ses dossiers — le dossier n'est supprimé que s'il perd son **dernier** formateur (sinon on détruirait les documents de l'apprenant et le travail des autres formateurs).
- **Choix du formateur pour un document** : un dossier pouvant compter plusieurs formateurs, `targetProf(g, id, user)` désigne celui que le document concerne (par défaut **soi-même** si on est formateur du dossier ; **400 explicite** si l'admin n'a rien précisé et qu'il y en a plusieurs). Utilisé par le **contrat de sous-traitance** (une extrémité = un formateur). Client : `GEN_PROF` + `curProf()`, sélecteur « Formateur concerné » affiché **uniquement s'il y en a plusieurs** en tête de la modale « Générer un document » (`genTargetsHTML`) ; l'`Interactive Worksheet` se préremplit au nom du formateur qui l'ouvre (`wsBlank(g, user)`), le **brouillon lui-même reste partagé** par tous les formateurs du dossier.
- **Confidentialité** : `groupView(g, me)` — l'apprenant voit **qui** est dans son dossier mais **pas la fiche** des formateurs (SIRET, NDA, adresse, date de naissance) : `pub` au lieu de `pubFull` pour les tiers. Seul l'**apprenant** peut remplir un QS ou signer une feuille de présence (ni formateur ni admin à sa place).
- **2 canaux par dossier** : `commun` (**tous** les formateurs + l'apprenant + admins) et `prive` (**tous** les formateurs du dossier + admins ; l'apprenant n'y a **aucun** accès lecture/écriture/download). ⚠️ un formateur ajouté après coup accède **rétroactivement** à l'historique privé. Chaque canal = **documents** (upload réel ≤25 Mo) **+ messagerie (chat)**.
- À l'envoi d'un doc/message, on choisit le **canal** (commun ou privé) via les onglets du dossier.
- **Admin centralisé** : tous les comptes admin voient la même chose (`/api/admin/overview` = tous les dossiers + comptes + documents). Admin peut tout voir/télécharger. **Suppression admin** : un dossier (`DELETE /api/groups/:id`, cascade = fichiers disque + docs + messages + qs + worksheets via `deleteGroupCascade`) ou un compte (`DELETE /api/users/:id` — apprenant : son dossier part avec lui ; formateur : il en est seulement **retiré**, cascade seulement si c'était le dernier). Boutons 🗑 + `confirmDialog` dans la vue globale.
- **Fiches profil** (remplies à l'inscription, stockées dans `user.profile`, exposées via `pubFull` dans `groupView`/`/api/me`) : **fiche apprenant** = tél, **société**, nb d'heures total + détail, intitulé de la formation, langue, **date de début + date de fin** (`dateDebut`/`dateFin`), **lieu** (distanciel/présentiel + adresse si présentiel : `lieu`/`lieuAdresse`), certification (oui/non) + texte si oui ; **fiche formateur** = langue, SIRET, NDA, adresse physique, tél, date de naissance, nationalité. Formulaire d'inscription à champs conditionnels selon le rôle (`collectProfile`).
- **Auto-remplissage des documents** depuis la fiche apprenant (`headerPrefill`/`clientFiche`/`certifLine`) : intitulé, langue, date de formation et **ligne « Certification »** pré-remplies sur les docs générés (QS, tests, worksheet via `wsBlank`). L'**Interactive Worksheet** pré-remplit aussi tél apprenant + **tél formateur** + langue depuis les fiches (`wsBlank` lit `E.profile`/`P.profile`).
- **Édition des fiches par l'admin** (même après création) : bouton ✏️ sur chaque apprenant/formateur de la vue globale → modale `openFicheEdit` (infos de base + fiche selon le rôle) → `PATCH /api/users/:id` (admin only, `cleanProfile`, e-mail unique vérifié). `/api/admin/overview` renvoie les users via `pubFull` (profils inclus).
- **Comptes démo** (seed au démarrage `ensureDemo`, jamais en double) : `prof@ls.fr` / `eleve@ls.fr`, mot de passe **demo1234** (+ dossier démo prof↔eleve). La fiche de `eleve@ls.fr` est remplie (certification incluse). ⚠️ **Plus AUCUN compte admin en démo** (05/08/2026) : `admin@ls.fr` est retiré de `DEMO_ACCOUNTS`. ⚠️ Le retrait du seed **ne supprime pas** un `admin@ls.fr` déjà en base : sur une base existante il faut le supprimer à la main (🗑 dans la vue globale).
- 🔒 **L'ENCART DE CONNEXION RAPIDE EST SUPPRIMÉ** (05/08/2026, demande de l'utilisateur). Il affichait sur la page de connexion **publique** l'adresse ET le mot de passe en clair des comptes démo. Sont partis : `demoBoxHTML()`, la variable `DEMO`, le câblage `.demo-login` et l'appel dans `loginForm()` (`account.js`), les 8 règles `.demo-*` (`site.css`), la clé i18n de l'encart dans les 5 dictionnaires, et surtout **la route `GET /api/demo-accounts`** — elle était **ouverte sans authentification** et distribuait les identifiants à qui la demandait (404 désormais). ⚠️ Ne pas la réintroduire. Les comptes démo restent seedés pour les essais, mais **plus rien ne les annonce** : il faut connaître `demo1234` pour s'en servir (et il reste lisible dans l'historique Git public).
- **Changer son mot de passe** (05/08/2026) : bouton **« Changer mon mot de passe »** dans `topHTML()` (tous les rôles, admin compris) → modale `openPasswordModal` (actuel + nouveau + confirmation, œil sur chaque champ) → **`POST /api/me/password {actuel, nouveau}`**. ⚠️ **C'était le trou** : la seule route qui posait un `passwordHash` était `POST /api/activate`, à **usage unique** — une fois le compte activé, plus personne ne pouvait changer son mot de passe, **pas même l'administration sur son propre compte**. La route exige le mot de passe **actuel** bien que la personne soit authentifiée (le jeton vit 30 jours dans un `localStorage` partagé entre onglets : un poste laissé ouvert suffirait sinon à verrouiller quelqu'un hors de son compte), refuse un nouveau mot de passe < 6 caractères ou identique à l'ancien, **agit toujours sur `req.user`** (aucun identifiant de cible : personne ne change le mot de passe d'un autre), et **ne déconnecte pas** l'onglet en cours. Elle lève aussi `mustActivate`/`activation` au passage, sinon un compte en attente resterait bloqué au 403 « compte non activé » avec un mot de passe pourtant valide.
- **Notifications** : message/doc reçu = notif (filtrée par canal). Cloche header (badge) → modale (fond flou) qui passe tout en lu. Les notifs portent un champ **`group`** (dossier d'origine, `notify(userId, text, group)`) : la case « Mes dossiers » affiche un **badge compteur** + fond/bordure accent (`.contact.has-new`/`.contact-badge`) sur chaque dossier ayant des notifs non lues ; **ouvrir le dossier consomme ses notifs** (`POST /api/notifications/clear-group {group}` → badge, couleur ET entrées de la cloche disparaissent).
- Header : déconnecté = « Se connecter » + « Créer un compte » (#creer ouvre l'inscription) ; connecté = cloche + prénom. **Menu hamburger mobile** (`partials.js` : `#mobile-menu` + `#nav-backdrop`) = drawer flou, croix, clic-dehors/Échap pour fermer, contient tous les boutons (espace docs en haut, nav, test/contact) ; sa section « Espace documents » est synchronisée à l'état connecté par `syncMobileMenu` (Se connecter/Créer un compte ↔ Mon espace/Se déconnecter, helper `logout`). En réduit, boutons espace-doc groupés à droite avec le burger.
- Non-admins ne voient/ajoutent pas les admins. Mots de passe : champ + confirmation, œil afficher/masquer.

### Visite guidée (tutoriel de première connexion) — 01/08/2026
- **Pour qui** : `prof` et `eleve` UNIQUEMENT. L'admin ne la voit jamais et n'a même pas le bouton (le calque n'est pas créé du tout pour lui, vérifié).
- **Déclenchement** : `POST /api/tuto/vu` (auth) enregistre `user.tutoVu = TUTO_VERSION` (entier, **premier niveau du user**). `tutoAVoir(u)` = non-admin ET `tutoVu < TUTO_VERSION`. ⚠️ Le champ n'est PAS dans `user.profile` : `cleanProfile` reconstruit `profile` par liste blanche à chaque `PATCH /api/users/:id`, il serait effacé au premier enregistrement de fiche. ⚠️ Il n'est PAS dans `pub`/`pubFull` (qui sérialisent aussi les **autres** membres d'un dossier et tous les comptes de l'overview) mais dans **`meFull(u)`**, exposé par **3 routes** : `/api/me`, `POST /api/login` et **`POST /api/activate`**. ⚠️ La 3ᵉ est la plus importante : l'activation par lien e-mail connecte automatiquement et alimente `ME` depuis sa propre réponse **sans jamais appeler `/api/me`** — n'enrichir que `/api/me` ferait rater exactement la première connexion visée (vérifié de bout en bout).
- **Contenu** : **8 étapes formateur**, **6 étapes apprenant**, dont 3 partagées mot pour mot (bienvenue, notifications, bouton de relecture). Tableaux `TUTO_PROF`/`TUTO_ELEVE` dans `account.js`.
- **Mécanique** : carte modale qui porte TOUJOURS le texte + **projecteur facultatif** (trou dans un voile, `box-shadow:0 0 0 9999px`). ⚠️ **Aucune étape n'est jamais supprimée** : ancre absente ou non montrable → projecteur éteint, carte centrée, même texte et même numéro. Sur un compte tout neuf sans dossier (le cas normal d'une première connexion), 4 des 8 étapes formateur sont centrées — vérifié.
- ⚠️ **La visite ne touche à RIEN** : elle n'écrit ni `selected` ni `channel`, n'appelle jamais `renderDashboard()` et ne clique jamais un `.contact` (cela déclencherait `clear-group` et **effacerait vraiment** les notifications non lues). Seul appel réseau : `POST /api/tuto/vu`, une fois, à la **première sortie quelle qu'elle soit** (Terminer, Passer, croix, Échap).
- ⚠️ **`tutoDessiner()` doit être appelé de façon SYNCHRONE** par `tutoPlacer`, pas seulement par la boucle `requestAnimationFrame` : rAF ne s'exécute pas dans un onglet en arrière-plan, le projecteur y restait éteint toute la visite (bug réel, trouvé au test).
- ⚠️ **Échap en phase de CAPTURE + `stopImmediatePropagation`** : trois écouteurs Échap coexistent en bouillonnement, dont `closeNotifModal` qui relance un `renderDashboard()` complet **sans vérifier** que la modale est ouverte. Vérifié : `#docspace` n'est pas reconstruit.
- ⚠️ **`document.body.style.overflow` n'est JAMAIS touché** (propriété inline partagée par 20 modales, `confirmDialog` et `body.menu-open`) : verrou dédié **`html.tuto-lock`**.
- ⚠️ **Ancres interdites** : `.qs-fill-btn`/`.pr-sign-btn` (n'existent que sur demande en attente et vivent dans `#chat-msgs`, cadre `max-height:300px;overflow:auto` forcé au bas — un halo s'y dessinerait sur du vide), `.doc-del`, `.docs`, `.ds-seeall`, `.mini-badge`, `.contact`, `#acct-bell-btn` (recréé toutes les 20 s), `.ds-main`/`.ds-grid`. `tutoCible()` rejette aussi : cible > 60 % de l'écran, hors écran, rognée par un ancêtre à défilement (⚠️ la remontée s'arrête **avant `body`**, qui porte `overflow-x:hidden` et rendrait tout inéligible), et moins de 200 px de place pour poser la carte.
- **Deux classes d'ancrage ajoutées** (`.ds-card-notifs`, `.ds-card-dossiers`) : aucune règle CSS ne les cible, apparence inchangée. Une ancre positionnelle (`:first-child`) se serait déplacée en silence à l'ajout d'une carte.
- **Accessibilité** : `role=dialog` + `aria-modal` + `aria-labelledby`, `aria-live=polite` sur le texte, **piège de focus** (sans lui, deux tabulations atteignent « Se déconnecter », invisible sous le voile — et Entrée déconnecte la personne). ⚠️ Au retour, `focus()` sur `<body>` ne déplace rien : si le focus est resté dans le calque, on le renvoie sur `.tuto-replay`.
- **i18n : 38 clés** (dont 1 déjà existante, « Générer un document ») × 5 langues. Le script de génération **lit les chaînes dans `account.js`** au lieu de les retaper — une clé qui diverge d'une virgule est morte en silence. ⚠️ Toute retouche du français impose de rouvrir les 5 dictionnaires.
- **Bouton « Revoir la visite guidée »** dans `topHTML()`, groupé avec « Se déconnecter » dans **`.ds-top-acts`** (sans ce conteneur, `justify-content:space-between` le ferait flotter au milieu du bandeau et déborder à 375 px). Il **n'écrit rien** en base.
- **Vérifié** : 375 px sans débordement sur toutes les étapes des 2 rôles, commandes à 45-47 px, `elementFromPoint` au centre de chacune ; russe (langue la plus longue) sans troncature, texte qui défile dans la carte ; admin sans visite ni bouton ; Échap/croix/Terminer persistent tous ; rechargement ne relance pas ; un `confirmDialog` qui apparaît referme la visite, et la visite refuse de s'ouvrir si un dialogue est déjà là.
- ⚠️ **À la mise en service, TOUS les formateurs et apprenants déjà en base verront la visite une fois** (`tutoVu` absent = falsy). C'est l'effet voulu. Et après chaque réinitialisation de `data/`, elle rejoue pour les comptes démo.
- **Corrections issues de la relecture adversariale (01/08/2026)** — 11 défauts confirmés sur 17 signalés, dont 7 portaient sur la **véracité du texte** ; chacun revérifié dans le code avant correction : (1) « tant que votre signature manque, l'envoi est refusé » était faux pour le **suivi d'assiduité** et la **feuille Test** (`if (!tpl.signAdmin && !sigImg(formateurSig))` — ces deux-là sont signées par l'administration) ; (2) l'onglet Historique ne **rouvre avec son contenu que l'Interactive Worksheet** (`reopenFromHistory` : `snapshot` pour `interactive`, formulaire neuf pour les 8 autres) ; (3) le préremplissage depuis la fiche n'a PAS lieu « à chaque génération » pour l'Interactive Worksheet (`wsFind(g.id) || wsBlank(...)` : le brouillon persistant gagne dès la 1ʳᵉ fois) ; (4) **aucun glisser-déposer n'existe** (zéro écouteur `dragover`/`drop` dans tout le dépôt) — le texte dit désormais « cliquez sur la zone de dépôt et choisissez un fichier ». ⚠️ **Le libellé de la zone elle-même (`docsBlock`) affirme toujours « Cliquez ou déposez un fichier » : c'est faux, et c'est antérieur à la visite guidée** — soit implémenter le dépôt, soit corriger le libellé (ce qui casserait sa clé i18n dans les 5 dictionnaires) ; (5) une **pièce signée n'est supprimable par personne** (`db.presences.some(p => p.docId === d.id)` → 400), alors que la corbeille reste affichée au formateur ; (6) la feuille e-learning n'a pas de séances à relire (`kind:'summary'`) ; (7) « moins d'une minute » pour 527 mots.
- ⚠️ **Vocabulaire des traductions** : une étape qui désigne un bouton doit l'appeler par son **libellé traduit du même dictionnaire**. Étaient incohérents : IT disait « discente » et « conversazione comune » là où l'écran affiche **Allievo** et **Discussione comune** ; RU disait « учащийся », « Общее обсуждение » et « Закрытый канал » là où l'écran affiche **Слушатель**, **Общий чат** et **Приватный**. Corrigé. Le script de génération **remplace** le bloc au lieu de le compléter, et vérifie l'absence de **doublons de clés** (dans un objet JS, la dernière gagne en silence).
- ⚠️ **`POST /api/tuto/vu` porte l'identité** (`{user: ME.id}`, 409 si elle ne correspond pas au jeton) : le jeton vit dans un `localStorage` **partagé entre onglets**, et `api()` le relit au moment de l'appel — un onglet resté ouvert pendant qu'un autre se connecte marquait « visite vue » chez la mauvaise personne. ⚠️ **La faiblesse est générale au client** (toutes les écritures s'identifient par le jeton ambiant : messages, notifications, signatures) ; seule cette route est protégée.

### API principales
`POST /api/signup|login`, `GET /api/me`, `GET /api/users`,
`GET|POST /api/groups`,
`GET|POST /api/messages?group=&channel=` (commun|prive),
`GET|POST /api/documents?group=&channel=` (+ `/:id/download?token=`),
`GET /api/notifications` + `POST /api/notifications/read|delete{id}|clear`, `GET /api/admin/overview`.
Données : `db.json` = `{users, groups, docs, messages, notifs, secret}`.
- **Notifications** : cloche **pollée toutes les 20 s** (`startNotifPoll`, le formateur voit en live quand l'apprenant répond, sans recharger). Modale = suppression **individuelle** (croix par ligne, `POST /api/notifications/delete`) + **« Tout supprimer »** (`POST /api/notifications/clear`).
- **E-mails de notification (SMTP OVH)** : module dans server.js (`mailConfig`/`sendMailSafe`/`mailHtml`, dépendance `nodemailer`). Config **HORS Git** (⚠️ jamais de mot de passe dans le code) : `data/smtp.json` `{host,port,secure,user,pass,from,siteUrl}` — prod/local : `ssl0.ovh.net:465`, auth + **expéditeur `"Languages & Success" <admin@languagesandsuccess.com>`** — ou variables d'env `SMTP_HOST/PORT/USER/PASS`, `MAIL_FROM`, `SITE_URL`. Sans config ou sans module → e-mails **désactivés proprement** (log `✉ … désactivés`, tout le reste fonctionne). Branché sur le **flux de signature des feuilles de présence** (les **3 types** elearning/presentiel/test passent par les 2 mêmes routes) : `POST /api/presence/send` → mail à l'**apprenant** « Un document à signer vous attend » (bouton → espace documents) ; `POST /api/presence/:id/sign` → mail au **formateur** (l'envoyeur `p.by`) « Document signé par X ». **Jamais d'envoi vers `@ls.fr`** (comptes démo, garde-fou dans `sendMailSafe`). Envoi **fire-and-forget** (ne bloque jamais la réponse API, erreurs loguées `✉`). Gabarit `mailHtml` = **carte type modal** : fond crème `#f8f2e7`, case `#fffaf0` bordure `#e6dccb` arrondie, **logo en pièce inline** (`cid:lslogo`, attaché automatiquement par `sendMailSafe` depuis `assets/ls-logo.png`), wordmark « Languages & Success » avec **seul le & en accent italique**, bouton accent, pied réduit à « Vous recevez cet e-mail… » + fallback texte. **En PROD** : pas de fichier sur le serveur — le workflow `deploy.yml` écrit `.env` depuis le **secret GitHub `ENV_FILE`** (repo → Settings → Secrets → Actions), auquel il faut ajouter `SMTP_HOST=ssl0.ovh.net` / `SMTP_PORT=465` / `SMTP_USER=admin@languagesandsuccess.com` / `SMTP_PASS=…` ; `docker-compose.yml` passe ces variables au conteneur (défaut expéditeur = `"Languages & Success" <SMTP_USER>`, `SITE_URL` par défaut). Variables vides → mails désactivés proprement.
- **Feuille de présence : le MOIS ne figure plus dans l'e-mail** (04/08/2026, demande de l'utilisateur). L'objet est `Feuille de présence à signer — Languages & Success` (fixe) et la phrase du corps ne nomme que le modèle. `fields.mois` reste renseigné et reste imprimé **sur le document**, mais il n'est plus repris nulle part dans le message.
- ⚠️ **La diffusion automatique sur les réseaux sociaux est SUPPRIMÉE** (05/08/2026, demande de l'utilisateur : « je publierai à la main »). Sont partis de server.js : `configSociale()`, `diffusionSociale()`, la lecture de `data/social.json` et l'appel à `graph.facebook.com` — plus les deux sites d'appel (route `POST /api/blog/articles/:id/publier` et boucle `tickProgrammation`). **Mettre un article en ligne n'émet plus AUCUNE requête sortante.** L'e-mail d'alerte « publication Facebook échouée » avait été retiré la veille (04/08) ; il est parti avec le reste. La modale de publication (`blog-admin.js`) ne promet plus de diffusion automatique, elle renvoie à la boîte des posts LinkedIn sous l'article. ⚠️ Ne pas réintroduire d'appel sortant à la publication sans le lui demander. (Contexte de l'époque, si la question revenait : seul Facebook était automatisable sans revue ; LinkedIn exige un produit validé et ne délivre pas de jeton durable ; TikTok refuse cet usage.)
- ⚠️ **Expéditeur IMPOSÉ : `nepasrepondre@languagesandsuccess.com`** (03/08/2026). Constante `MAIL_EXPEDITEUR` dans server.js, **qui ignore `MAIL_FROM` et le champ `from` de `data/smtp.json`** : un `MAIL_FROM` oublié dans l'`ENV_FILE` de production ferait autrement repartir les mails de l'ancienne adresse sans que personne s'en aperçoive. L'**authentification SMTP reste `admin@languagesandsuccess.com`** ; OVH accepte cet écart entre compte authentifié et expéditeur (vérifié, réponse 250). ⚠️ Si OVH durcissait sa politique, il faudrait créer la boîte ou l'alias `nepasrepondre@` côté OVH.
- **Mention « message automatique » sur TOUS les envois** : ajoutée par `composerMail`, partagée par `sendMailSafe` et par le test d'envoi — aucun appelant ne peut l'oublier. En texte brut (`MAIL_NOREPLY`) et dans un encadré du gabarit HTML, avec renvoi à `contact@languagesandsuccess.com`. En-têtes `Auto-Submitted: auto-generated` et `X-Auto-Response-Suppress: All` pour couper les réponses d'absence.
- **Test d'envoi** : `POST /api/admin/mail-test {to}` (admin only). Contrairement à `sendMailSafe` qui n'échoue jamais bruyamment, cette route **attend** le résultat SMTP et le renvoie. ⚠️ Elle passe par `composerMail`, donc elle teste le vrai chemin : un test qui aurait sa propre composition ne vérifierait que lui-même.
- **Sauvegarde OFFSITE quotidienne** (module dans server.js : `backupCfg`/`buildDataArchive` (mini-tar POSIX + gzip, zéro dépendance)/`runOffsiteBackup`/`offsiteTick`) : archive `ls-data-<date>.tar.gz` de `data/` (db.json + uploads + smtp.json ; **exclus** : backups/ locaux, db.json.tmp, backup.json) envoyée ~1×/jour sur **Backblaze B2** (API native via fetch), rétention 30 archives. Config **HORS Git** : env `B2_KEY_ID`/`B2_APP_KEY` (+ `B2_BUCKET_ID` si clé non limitée au bucket — sinon bucketId auto via `allowed`) ou `data/backup.json` `{keyId,appKey}` / `{local:"chemin"}` (mode disque local, utilisé pour les tests). Sans config → **désactivée proprement** (log `🗄 …`). **Cadence : 4×/jour à 8/12/16/20 h heure de Paris** (`BACKUP_SLOTS` + `parisHour` via `Intl` — le conteneur est en UTC ; tick toutes les 15 min, garde 3,5 h entre 2 envois, robuste aux redéploiements). **Rétention** : toutes les archives des 3 derniers jours + la dernière de chaque jour sur 30 jours (les 5 plus récentes sont toujours gardées). Statut persisté `db.backupStatus` (dont `users`/`docs`). Routes admin : `GET /api/admin/backup-status`, `POST /api/admin/backup-run` (`{force:true}` pour passer outre le garde-fou). Tout est en try/catch : un échec ne touche jamais le site.
  - **Durcissements issus de l'audit adversarial (25/07/2026)** — ⚠️ ne pas régresser : (1) `addFile` **borne la lecture à la taille annoncée** (`createReadStream {start,end}`) et complète par des zéros si le fichier a rétréci — sans ça, un fichier qui grossit pendant l'archivage (téléversement multer, qui écrit directement à l'emplacement final) désynchronise le flux tar et **fait disparaître en silence tous les fichiers suivants** ; (2) `done.catch()` + drapeau `streamErr` : une erreur d'écriture ne peut plus produire un rejet orphelin qui **tuerait le process** ; (3) noms > 100 octets : champ `prefix` ustar, sinon **erreur explicite** (jamais de troncature silencieuse) ; (4) **garde-fou anti-base-vide** : refus si le nombre de comptes s'effondre (< 50 % de la dernière sauvegarde réussie) — sinon un volume non monté ferait tourner la rétention sur des archives vides ; (5) rétention **filtrée sur le motif exact** des archives + pagination `nextFileName` + jamais moins de 5 archives ; (6) **refus des buckets publics** (`bucketType !== allPrivate`) : l'archive contient données personnelles, hachages, `db.secret` et le mot de passe SMTP ; (7) SHA1 en flux + refus au-delà de 350 Mo (pas d'OOM) ; (8) `AbortSignal.timeout` sur tous les appels B2 ; (9) **alerte e-mail à l'admin** en cas d'échec ou d'absence de sauvegarde > 48 h (`alertBackupFailure`, max 1/24 h) ; (10) nettoyage des archives temporaires orphelines au boot + refus d'une destination locale située dans `data/`. **Restauration** : télécharger l'archive B2 → `tar -xzf ls-data-….tar.gz` → recopier le contenu de `data/` extrait dans le volume. ⚠️ lecture des JSON de config robuste au BOM (fichiers créés sous Windows).
- **Déploiement / données prod** : `docker-compose.yml` monte le **volume nommé `ls-data` → `/app/data`** (ajouté le 21/07/2026 — avant ça, chaque push **effaçait** toutes les données de l'espace documents en prod, conteneur reconstruit sans volume). Les données prod survivent désormais aux déploiements ; c'est ce volume que la sauvegarde offsite du déployeur doit archiver (`docker run --rm -v <projet>_ls-data:/data …`).
- **Noms de fichiers générés** : assainis côté serveur via `safeFile()` + `nameDate()` (date `jj-mm-aaaa` sans « / », sinon le navigateur tronquait le nom à « 2026 »).
- **Vue admin globale** : 3 filtres **multi-sélection** (`adminShow` = Dossiers / Comptes / Fichiers, tous actifs par défaut) + barre de recherche `norm()` ; sections empilées (dossiers+leurs docs, comptes, **liste à plat de tous les fichiers** avec `groupLabel`).
  - **Fichiers d'un dossier repliables** (30/07/2026) : bouton `.adm-fold` (compteur + chevron) dans l'en-tête de chaque dossier qui en contient, + bouton global **« 📄 Replier / Déplier tous les fichiers »** (`.adm-foldall`) dans la barre admin — avec beaucoup de dossiers bien remplis la vue était illisible. État en mémoire (`admFilesHidden`, pas de `localStorage`), qui **survit aux re-rendus** du panneau (filtrage, suppression) mais pas au rechargement. ⚠️ Le pli **ne passe PAS par `rerenderAdmin`** : on bascule la classe `.files-off` sur place, sinon le panneau est reconstruit et la transition CSS n'a pas lieu. **Animation** : `.adm-files` est une grille dont la rangée passe de `1fr` à `0fr` (c'est ce qui permet d'animer une hauteur inconnue sans la mesurer en JS), l'enfant porte `overflow:hidden;min-height:0`, + fondu d'opacité et chevron qui pivote. ⚠️ Les 3 boutons de l'en-tête sont groupés dans `.adm-grp-acts` (+ `flex-wrap` sur `.adm-grp-h`) : sans ça le 3ᵉ bouton faisait **déborder l'en-tête de 26 px à 375 px**. Dans la **case dossier**, l'**apprenant est affiché avant le formateur** (`membersChips`, `groupTitle`, libellé admin). Notifications : **singulier/pluriel** corrects (« 1 notification non lue » / « 2 notifications non lues »).
- **Pied de page de TOUS les docs générés** (`metaLines(user, ver)` injecté dans `pdfHeaderFooter`/`docxHeaderFooter`) : « Créé le 07/06/2026 par FPE », « Rédigé le {date de génération} par {compte générateur} », **la version** (à gauche) + bloc légal `LEGAL_LINES` (à droite).
- **VERSION qui évolue** (29/07/2026) : `bumpVersion(g, tpl)` → **1.0** à la première génération d'un document, puis **+0,1 à chaque nouvelle génération** du même document (1.1, 1.2 … 1.9, 2.0). Compteur **persistant** `db.docVersions['{groupId}|{tpl}']`, volontairement **indépendant de `db.docgens`** qui est purgé au-delà de 40 entrées par dossier. Chaque modèle a son propre compteur dans un même dossier. Le texte reste « Ce fichier n'a pas encore été modifié — Version 1.0 » en 1.0, puis devient « Version X.Y ». ⚠️ La version est passée **en paramètre explicite** (`build*(…, user, ver)` → `pdfHeaderFooter(doc, user, ver)`) et non par une variable globale : les constructeurs sont asynchrones, deux générations simultanées se mélangeraient. ⚠️ **Tout nouveau générateur doit appeler `bumpVersion` et transmettre `ver`**, sinon son pied de page restera figé à 1.0. À noter : générer le même document en PDF puis en Word compte pour **deux** versions.

## Blog — articles en base, pilotés depuis le site (section réécrite le 05/08/2026)
> ⚠️ **Cette section a été refaite parce qu'elle décrivait un outillage disparu.** Le commit `b054aa8` « le blog passe en base » a supprimé `blog/README.md`, `blog/outils/blog.js` (367 lignes), `blog/outils/notifier.js`, `blog/outils/gabarit.html`, `blog/brouillons/`, le dernier article en HTML **et `sitemap.xml` lui-même**, contre +373 lignes dans `server.js` ; `blog/outils/couverture.js` est parti au commit `2039884`, remplacé par `visuels.js`. **Ne cherchez aucune de ces commandes : `blog.js aecrire|proposes|valider|refuser|planning|publier|depublier|index|sitemap` et `couverture.js` n'existent plus.** Les **deux tâches planifiées Cowork** (`blog-ls-1-recherche-sujets`, `blog-ls-2-redaction`) **n'existent plus non plus** : `list_scheduled_tasks` ne renvoie rien (vérifié le 05/08/2026).
- ⚠️⚠️ **UN ARTICLE EST UNE DONNÉE, PAS DU CODE — `git push` ne l'emporte JAMAIS.** Les articles vivent dans `db.articles` (fichier `data/db.json`, volume `ls-data`), le dépôt ne contient que le moteur. Un article créé en local n'existe que sur ce poste : l'utilisateur regarde le site EN LIGNE et ne voit rien. **Erreur commise deux fois** (31/07 et 03/08/2026). D'où la cible par défaut de `creer.js` : la **production**. ⚠️ **Les VISUELS, eux, sont du code** (`blog/img/*.png`) : ils ne partent qu'avec le déploiement — créer l'article en prod sans committer les PNG laisse des images mortes le temps du déploiement. `creer.js` le rappelle à chaque exécution.
- **Où vit quoi** : les articles en base ; `blog/img/*.png` les visuels (dans Git) ; `blog/outils/` l'outillage local (`creer.js`, `posts-li.js`, `visuels.js`, `sync-prod.js`) ; `blog/posts-linkedin.js` une copie des posts hors base ; `blog/articles-sources/` les fichiers de définition ; `blog/sujets.md` un **carnet d'idées que plus aucun code ne lit** (il le dit lui-même en tête). ⚠️ `articles-sources/` **n'est PAS une archive** : 1 définition pour 5 articles. Réinitialiser `data/` sans sauvegarde perd les 4 autres définitivement. ⚠️ `sujets.md` **n'est plus synchronisé avec la base** : ses 10 sujets sont tous marqués `proposé` alors que plusieurs sont déjà rédigés et existent en brouillon. **Vérifier la base avant d'écrire**, sinon on réécrit un article existant. Ses statuts (`proposé` / `refusé`) ne valent plus que comme notes de lecture ; `refusé` reste utile comme **mémoire** : ne jamais reproposer un sujet refusé, même reformulé.
- ⚠️ **RIEN NE SE RÉGÉNÈRE PLUS.** La grille de `blog.html` est remplie **à chaque requête** par le serveur entre `<!-- ARTICLES:DEBUT/FIN -->` (`blogIndexHtml`, server.js) — les marqueurs subsistent, mais comme repères de découpage à la volée. `sitemap.xml` **n'est plus un fichier**, c'est une route qui liste les pages `.html` de la racine plus les articles en ligne. La page d'un article est construite en mémoire par `artPage`. Vérifié de bout en bout le 05/08/2026 : publier un article le fait apparaître pour un visiteur anonyme dans la grille **et** dans le sitemap (12 → 13 URL) instantanément ; le dépublier l'en retire. Aucune commande à lancer, jamais.
- **Les 8 routes** (`/api/blog/articles`) : `GET` liste et `GET /:id` sont **ouvertes sans authentification** ; les 6 écritures (`POST`, `PATCH`, `DELETE`, `POST /:id/publier|dupliquer|depublier`) exigent `auth` **puis `adminSeul`** — un formateur ou un apprenant connecté reçoit 403. Un article est **toujours créé en brouillon** (statut imposé par le serveur) et `PATCH` **ne peut pas publier** (`statut` hors liste blanche). `publier` fait les trois cas : sans date → en ligne tout de suite, date future → `programme`, date passée → en ligne. `depublier` remet en brouillon sans rien supprimer.
- **Visibilité** : `artVisiblePar` = en ligne pour tous, sinon **rôle admin exigé**. ⚠️ Un article resté au statut `programme` **dont l'heure est passée est déjà en ligne** : `artEnLigne` compare la date à chaque requête, la bascule de statut (minuteur `tickProgrammation`, toutes les 60 s) ne fait que régulariser le champ. ⚠️ Demander l'adresse d'un brouillon sans `?token=` ne donne **pas une 404** mais **200 + la page d'accueil** (soft 404 de tout le site) : d'où `suivre()` plutôt qu'un `location.reload()` après une action, le slug étant modifiable.
- **L'interface admin** (`assets/blog-admin.js`, chargé pour tout le monde, n'écrit que si `/api/blog/articles` répond `admin:true`) : barre **« Administration du blog »** sur `blog.html` avec **« + Nouvel article »**, et la même barre sur la page d'un article via l'ancre `<div id="ls-art-adm" data-art="…">`. Les commandes sont **cinq à la fois** (jamais six : les conditions s'excluent) — Publier / Programmer / Repasser en brouillon selon l'état, puis toujours Modifier, Dupliquer, Supprimer. Chacune passe par la modale maison `dialogue()`, jamais par un `confirm()`. Sur `blog.html` l'admin voit la grille **reconstruite côté navigateur** (brouillons inclus) par-dessus celle du serveur ; la sécurité tient côté serveur, pas là.
- ⚠️ **Le tiret cadratin (—) est PROSCRIT du contenu publié** : articles ET posts LinkedIn (demande de l'utilisateur, 03/08/2026). Selon le contexte : deux-points si la suite explique, virgule si elle prolonge, point si c'est une phrase nouvelle, parenthèses pour une incise. **Bloquant** dans **`blog/outils/creer.js`** (titre, chapô, titreSeo, metaDescription, corps, FAQ, sources) et dans **`blog/outils/posts-li.js`** (les trois posts). Les commentaires de code ne sont pas concernés. 49 occurrences retirées des 5 articles existants, en local et en production.
- ⚠️ **RÈGLE : générer un article, c'est générer ses TROIS posts LinkedIn.** Jamais l'un sans l'autre, sans que l'utilisateur ait à le demander (exigé le 03/08/2026). Ce n'est pas une consigne de mémoire : **`blog/outils/creer.js` REFUSE de créer l'article** si `def.postsLi` n'a pas exactement 3 versions conformes — rien n'est créé, l'erreur liste ce qui manque. Les règles et leur contrôle sont dans **`blog/outils/posts-li.js`** (`verifierPosts`).
  - **Trois angles d'accroche** : `La question` · `Le chiffre` · `Le terrain`. Champ `postsLi: [{angle, texte}]` sur l'article, **admin only** (supprimé de la réponse pour tout autre rôle, y compris sur un article publié — vérifié).
  - **Bloquant** : accroche en 1ʳᵉ ligne ≤ 210 caractères (LinkedIn replie au-delà), mot-clé exact de l'article **avant le caractère 250**, 3 hashtags minimum, **aucun lien dans le corps** (LinkedIn réduit la portée des posts sortants → renvoi au premier commentaire). **Alertes** : > 5 hashtags, pas de renvoi au commentaire, > 2600 caractères, phrase longue répétée.
  - ⚠️ La comparaison du mot-clé doit **normaliser l'apostrophe typographique** (`’` → `'`) : le mot-clé d'un article en porte une, le texte des posts non. Sans ça le contrôle déclare « absent » ce qui est présent (erreur réellement commise).
  - Les posts vivent en base (`db.articles[].postsLi`) et sont **aussi versionnés dans `blog/posts-linkedin.js`**, la base n'étant pas dans Git. L'édition se fait dans la boîte sous l'article (`.li-box`, 3 blocs, un bouton Copier chacun), pas dans la modale de modification.
  - ⚠️ **La barrière ne tient QUE sur le chemin `creer.js`.** Le bouton **« + Nouvel article »** de l'interface poste directement l'article : le serveur n'exige que le titre, `postsLi` part vide, et **aucun contrôle du tiret cadratin n'existe côté serveur**. De même, le `PATCH` de la boîte LinkedIn enregistre n'importe quel texte sans repasser par `verifierPosts`. Conséquence pour moi : **toujours créer un article par `creer.js`**, jamais par l'API directe — c'est ce passage obligé qui rend la règle automatique. (L'utilisateur, lui, clique où il veut : c'est son site.) En revanche `PATCH` **n'efface pas** les posts existants, il ne touche `postsLi` que s'il en reçoit un tableau.
- **Bouton « Dupliquer »** (les deux vues) : `POST /api/blog/articles/:id/dupliquer` (admin only) crée une copie **en brouillon** titrée « Copie — {titre} » (slug dérivé, unique), FAQ, sources et posts **copiés en profondeur** (sinon les deux articles partageraient les mêmes objets), et l'UI ouvre la copie. ⚠️ Le champ `image` est copié **tel quel** : la copie affiche les visuels de l'original, alors que les fichiers sont nommés d'après le slug, qui a changé. Regénérer les visuels au nouveau slug si la copie doit vivre sa vie.
- ⚠️ `.post-acts button` passe à **40 px de haut sous 620 px** : « Supprimer » voisinait « Dupliquer » à 32 px. Desktop inchangé (32 px).
- ⚠️ **UNE SEULE PORTE, ET ELLE EST HUMAINE : le bouton « Publier ».** La règle de fond posée le 31/07/2026 (« rien ne s'écrit et rien ne se publie sans l'accord de l'utilisateur », après son refus explicite d'un enchaînement automatique) **reste entière**, mais elle n'a plus aucun support automatique : les deux tâches Cowork qui l'incarnaient n'existent plus, et l'outil que la seconde appelait a été supprimé. Concrètement : **je ne rédige un article que si l'utilisateur me le demande**, il arrive en brouillon, et **lui seul le met en ligne**. Depuis le 04/08/2026 la mise en ligne n'émet plus non plus **aucune requête sortante** : les posts LinkedIn sont **rédigés** mais **publiés à la main** par l'utilisateur, depuis la boîte sous l'article.
- **Notifications Slack `#blog_claude`** (id `C0BM1SQ81B7`) : l'outil MCP Slack, **depuis une exécution d'agent uniquement**. ⚠️ Le repli `blog/outils/notifier.js` **n'existe plus** (supprimé au commit `b054aa8`) et `data/slack.json` n'existe pas : il n'y a **aucun moyen de notifier depuis un script shell**. Rien ne remplace l'outil MCP.
- **SEO** (les règles vivaient dans `blog/README.md`, supprimé — elles sont ici désormais) : un mot-clé principal par article, présent dans le `<h1>`, le premier paragraphe et au moins un `<h2>` ; `<title>` ≤ 60 caractères, meta description 150-160 ; 2 à 3 liens internes. `canonical`, Open Graph, Twitter card et le **JSON-LD `Article` + `BreadcrumbList` + `FAQPage`** sont posés **par le serveur** (`artPage`), il n'y a rien à écrire à la main. ⚠️ L'ancienne mise en garde sur le JSON-LD de la FAQ (« reprendre exactement les questions visibles ») **n'a plus d'objet** : les deux sont générés du même tableau. La règle qui la remplace : **le `<h1>` est posé par le serveur**, donc le champ `corps` ne doit contenir que des `<h2>`/`<h3>` — un second `<h1>` casserait la hiérarchie, et **rien ne le vérifie**.
- **FAQ obligatoire** : 5 questions (4 au minimum), saisies dans le champ `faq` — le serveur les rend en `<h3>` dans un `<div class="faq">` et les reprend dans le JSON-LD.
- 🔒 **LE SITE NE SERT PLUS QUE LE SITE** (05/08/2026) — voir la liste `PRIVE` en tête de server.js. Le Dockerfile copie le dépôt **entier** dans l'image et `express.static` servait tout ce que le filtre ne bloquait pas ; or ce filtre ne couvrait que `data/`, `node_modules`, `server.js` et `package.json`. Étaient donc publiquement téléchargeables en production : **`/blog/outils/sync-prod.js` et `/creer.js`, avec l'identifiant ET le mot de passe du compte admin de production en clair** ; **`/CLAUDE.md`, qui porte les mêmes mots de passe** et toute l'architecture ; `/blog/posts-linkedin.js`, les notes internes que l'API retire pourtant à tout non-admin ; `/blog/sujets.md`, `/blog/articles-sources/`, `/Dockerfile`, `/docker-compose.yml`, `/RESTORE.md`, `/.github/workflows/deploy.yml`, `/.gitignore`, `/versions/`. Tout cela répond 404 désormais. **Restent publics** : les pages `.html`, `assets/`, `blog/img/` (visuels des articles), `robots.txt`, et les scripts de la racine que les pages chargent (`ls-engine.js`, `test-data.js`, `morph.js` et les animations en réserve). ⚠️ **Toute nouvelle ressource servie doit être vérifiée contre `PRIVE`** — la règle `/\.md$/` bloque tous les fichiers Markdown, la règle `/^\/\./` tous les fichiers cachés. ⚠️ `docker-compose.yml` ne contenait **que des références** de variables, aucune valeur : aucun secret d'infrastructure n'a fuité par là (vérifié). ⚠️ **Cela ne règle que l'exposition par le site** : le dépôt GitHub est public, le mot de passe admin y reste lisible tant qu'il n'est pas **changé et sorti du code** (piste : le lire dans `LS_MDP_PROD` sans valeur de repli, et le sortir aussi de ce fichier).
- ⚠️ **Pièges de l'outillage, tous vérifiés le 05/08/2026** : (1) relancer `creer.js` avec un slug déjà pris **ne met pas à jour** l'article, le serveur rend le slug unique en le suffixant et crée un **doublon** dont le champ `image` pointe encore sur les visuels du premier ; (2) `sync-prod.js` lit `blog/posts-linkedin.js` par un **chemin absolu `C:/Users/luluz/…`** (il ne tourne que sur ce poste), fait **gagner ce fichier sur la base** — une correction faite dans la boîte `.li-box` est donc silencieusement écrasée à la synchronisation suivante — et **ne transporte pas le statut**, tout arrive en brouillon en production ; (3) le troisième argument « Titre » documenté en tête de `visuels.js` **n'est pas lu** : le CLI ne prend que le slug et la catégorie ; (4) `robots.txt` interdit toujours `/blog/brouillons/`, répertoire supprimé — inoffensif, mais c'est un vestige ; (5) trois PNG orphelins `blog/img/essai-barriere-a-supprimer*.png` traînent dans le dépôt et sont servis en ligne.
- **Visuels** : `node blog/outils/visuels.js <slug> "<Catégorie>"` écrit **trois PNG** (dépendance `jimp`) — `blog/img/<slug>.png` la couverture 1200×630 (vignette, tête d'article, `og:image`) et `<slug>-a.png` / `<slug>-b.png` deux illustrations de section 1200×500. Composition **déterministe**, dérivée du slug : même article = même image. `creer.js` les génère tout seul et remplace `{{ILLU_A}}` / `{{ILLU_B}}` dans le corps. **En PNG et non en SVG** : aucune plateforme sociale n'accepte le SVG comme image de partage. ⚠️ À **committer** : ce sont les seuls fichiers du blog qui voyagent avec le déploiement.
- ⚠️ **`.post img.thumb` DOIT porter `height:auto`** : l'attribut HTML `height="630"` de la vignette est une indication de présentation qui l'emporte sur `aspect-ratio`, et la carte afficherait une image de 630 px de haut. Les attributs `width`/`height` restent utiles (ils réservent la place et évitent le saut de mise en page).
- **Créer un article** : `node blog/outils/creer.js <définition.js>` — la définition exporte `{titre, chapo, categorie, motCle, slug, titreSeo, metaDescription, corps, faq[], sources[], postsLi[]}` (modèle : `blog/articles-sources/`). ⚠️ **La cible par défaut est la PRODUCTION** (`CIBLE_DEFAUT`, surchargeable par `LS_BASE`), les identifiants étant choisis automatiquement selon la cible ; pour travailler en local il faut le demander explicitement : `node blog/outils/creer.js <def.js> http://localhost:8000`. L'outil génère les visuels, vérifie les trois posts et le tiret cadratin **avant** de créer quoi que ce soit, puis passe une série de contrôles SEO non bloquants (longueurs, FAQ, sources, mot-clé dans le corps, présence d'un `<h2>`). Il ne publie **jamais** : l'article arrive en brouillon. `blog/outils/sync-prod.js` recopie en production ce qui n'y est pas encore (voir ses pièges plus haut).
- ⚠️ **`partials.js` est désormais conscient de la profondeur** : les articles sont dans `/blog/`, or le header et le pied sont écrits en chemins relatifs (`index.html`, `assets/…`) qui pointaient donc sur `/blog/index.html`. `versRacine()` préfixe `../` à l'injection (URL absolues, `mailto:`, `tel:` et ancres épargnées), et les scripts injectés (`i18n.js`, `account.js`) aussi. Toute nouvelle page en sous-dossier en bénéficie automatiquement.
- Les articles restent **en français** (comme les pages légales) : aucune clé i18n à ajouter.

## SEO du site (31/07/2026)
- **Les 13 pages portent** : `canonical`, Open Graph (9 balises) + Twitter card, `theme-color`, et un **JSON-LD `@graph`** = `EducationalOrganization`+`LocalBusiness` (`@id` `#organisme`, adresse de Nice, tél, e-mail, SIRET/RNA/NDA, Qualiopi `CERT_S0226_0162`, `areaServed`, `sameAs` LinkedIn/Facebook) + `BreadcrumbList` ; `index.html` ajoute `WebSite`. `espace-documents.html` est en **`noindex,follow`**.
- **`robots.txt`** est un fichier statique, jamais régénéré (il bloque `/espace-documents.html`, `/data/`, et encore `/blog/brouillons/` qui n'existe plus). **`sitemap.xml` n'est PAS un fichier** : c'est une **route** de server.js qui liste, à chaque appel, les pages `.html` de la racine (sauf `espace-documents.html`) plus les articles en ligne — un nouvel article y entre donc tout seul à sa mise en ligne, sans aucune commande.
- **Image de partage** : `assets/og-cover.png` (1200×630, 159 ko), fabriquée avec jimp. ⚠️ **En PNG et pas en SVG** : LinkedIn/Facebook/WhatsApp ne lisent pas le SVG comme `og:image`.
- **Poids des pages divisé par ~5** : les logos du pied de page étaient servis en pleine résolution d'impression (`charte-cpf.png` 2333 ko, `datadock.png` 1738 ko, `cpf-logo.jpg` 228 ko) alors qu'ils s'affichent à 84 et 68 px de haut. Redimensionnés au double de la taille d'affichage → **4298 ko → 123 ko sur CHAQUE page**. Les originaux restent dans l'historique Git.
- ⚠️ **`assets/ls-logo.png` (192 ko) n'a PAS été touché** : `morph.js` l'échantillonne pour l'animation du héros (le réduire changerait le visuel) et `server.js` s'en sert pour l'en-tête des PDF et l'e-mail. `assets/ls-seal.png` (7000×7000, 1,6 Mo) n'est utilisé par aucune page — poids mort dans le dépôt, sans effet sur la vitesse.
- **Hiérarchie des titres corrigée** : plus aucun saut de niveau. Les `h4` d'`index`/`formations`/`entreprises` sont devenus des `h3`, celui de `contact` un `h2`. ⚠️ Les règles `.testcard h4`, `.spec .cell h4` et `.aside-card h4` ont été renommées **et complétées de `font-weight:700;line-height:1.62;letter-spacing`** : sans ça la règle de base `h1,h2,h3` (poids 500, interligne 1.04) aurait changé leur apparence, ce dont un `h4` était exempt. Apparence vérifiée identique sur les 10 titres.
- **Écarté par l'utilisateur (ne pas y revenir sans lui demander)** : ajouter une FAQ sur `financement.html`/`entreprises.html`, raccourcir les 5 `<title>` > 60 car. et les 8 meta descriptions hors cible, reformuler le `h1` « Votre formation financée à 100 % » de `financement.html`.
- ⚠️ **Les traductions n'apportent RIEN en référencement** : le site est traduit côté navigateur à la même adresse, donc Google ne voit que le français et aucun `hreflang` n'est possible. Ranker à l'étranger demanderait des URL distinctes par langue (chantier structurel, non engagé).

## Test de niveau (`test-de-niveau.html` + `test-data.js`)
- 12 langues × 10 questions graduées A1→C2 dans `test-data.js` (`{t, o, a, e}` ; `e` = explication FR).
- Landing : section test → choix de langue → 1re question dans la langue → redirige vers `?lang=`.
- Résultat : niveau CECRL + **compétences du niveau** + **correction détaillée** (réponse, bonne réponse, explication).

## Mobile (audit 375 px du 30/07/2026)
- **Champs de saisie : 16 px imposés sous 620 px** (fin de `assets/site.css`). Safari iOS **agrandit la page** dès qu'on touche un champ dont la police fait moins de 16 px, obligeant à repincer ; les 13 champs du site étaient entre 13,5 et 15 px (contact, test de niveau, connexion) comme tous les champs des modales de génération. ⚠️ Les deux `:not()` du sélecteur ne filtrent rien : ils portent la **spécificité** au-dessus de `.form input` / `.gf input` / `.quiz-card .field select`, sinon la règle serait sans effet. Desktop inchangé (tailles fines conservées).
- ⚠️ **`.ds-grid` en mobile = `minmax(0,1fr)` et NON `1fr`** : le minimum implicite de `1fr` est le `min-content` des cartes, donc un contenu un peu large fait **déborder la colonne de son conteneur** au lieu de le comprimer. C'est ce qui est arrivé en passant les champs à 16 px (colonne 339 → 360 px dans un conteneur de 311 px). Ne pas régresser.
- **Mesure fiable de débordement** : `documentElement.scrollWidth - clientWidth` dans le vrai onglet est **faussé de ~17 px** par la barre de défilement du bureau (`innerWidth` ≠ `clientWidth`) ; les barres sont en surimpression sur un vrai téléphone. Mesurer dans une **iframe** donne le chiffre juste — ⚠️ **mais l'iframe se dote elle aussi d'une barre classique qui vole 15 px** : une iframe de 375 de large n'offre que **360 px** de mise en page et invente des débordements de ~13 px (piège réellement rencontré le 05/08/2026 sur `formations.html` et `contact.html`, tous deux à 0 une fois mesurés correctement). **Ouvrir l'iframe à 390 px** et vérifier que `clientWidth` vaut bien 375 avant de conclure. ⚠️ Dans le panneau MCP non affiché, `innerWidth` et `clientWidth` valent **0** et toute mesure est absurde : l'iframe est alors la seule voie. Écarter aussi le tiroir `#mobile-menu` (hors écran par construction) et tout ce qui est dans un conteneur `overflow-x` (bandeau de logos, canvas du héros).
- Vérifié au 30/07/2026 : **13 pages à 0 débordement**, héros dont la barre de chiffres ne chevauche pas l'animation, tiroir mobile (ouverture, fond flou, clic-dehors, Échap), grille des 6 langues (3 colonnes), formulaires de génération et feuille de présence sans débordement.
- **Zones tactiles agrandies sans toucher à l'apparence** (30/07/2026) : un `::after` transparent en position absolue élargit la surface cliquable sans occuper de place dans le flux — hamburger 36 × 28 → **56 × 44**, croix du tiroir 19 × 32 → **45 × 44**, liens de nav 34 → **44** de haut, boutons de langue 33/35 → **44/46** (l'écart **vertical** de `.mm-lang-grid` a été porté à 11 px pour ça, l'horizontal restant à 8 px — l'augmenter rétrécirait les boutons, la grille faisant 3 colonnes à largeur fixe). Débords calculés pour **ne jamais empiéter sur la cible voisine** (6 px restent entre deux liens ; 6 px entre la zone du burger et le prénom dans l'en-tête de l'espace documents). Vérifié par `elementFromPoint`, pas seulement au calcul. ⚠️ **Ne PAS mettre `position:relative` sur `.mm-close`** : elle est déjà `absolute` (coin haut-droit), et `relative` la remet dans le flux — elle passe à 252 px de large et pousse tout le contenu du tiroir. Un élément déjà positionné sert de référence à son `::after`.
- ⚠️ **`.header-cta` habille aussi un vrai `<button>`** (« Se déconnecter » du tiroir) : il doit donc déclarer `color`, `background`, `appearance:none` et `line-height:inherit`. Sans ça le navigateur impose son habillage — fond gris + texte noir sur ordinateur, texte **bleu système sur iPhone** — et un bouton 7 px plus court que les `<a>` voisins. Règle générale : tout style partagé entre `<a>` et `<button>` doit poser ces quatre propriétés, un `<a>` héritant de l'encre là où un `<button>` ne le fait pas.
- Le **changement de langue depuis le tiroir le referme** (`setMenu(false)` si le bouton cliqué est dans `#mobile-menu`), comme un lien de navigation.
- ⚠️ **Mesurer une transition CSS dans la preview MCP** : quand le panneau n'est pas au premier plan, les transitions **ne progressent pas** (un tiroir peut porter la classe `open` tout en restant à `translateX(100%)`). Terminer les animations à la main (`el.getAnimations().forEach(a => a.finish())`) avant de mesurer, ou fronter l'onglet (`tabs_select`) — sinon on mesure un état intermédiaire figé et on conclut à tort à un bug.

## Conventions / préférences utilisateur
- Réponses en **français**. L'utilisateur teste sur **localhost:8000** (Ctrl+Shift+R).
- Accent orange-rosé : `--accent:#be6e54`. Jamais le mot « promesse ». « apprenant » (pas « stagiaire »).
- Boutons orange-rosé scintillent ; liens « politique de confidentialité » inline (pas rejetés à droite) ; boutons d'envoi centrés.
- Après une modif, **vérifier** (eval/screenshot via preview MCP `site` sur 8011, ou tests `node -e` contre l'API), puis **réinitialiser `data/` et relancer le serveur 8000 propre** — en **conservant `data/smtp.json`** (config e-mails, hors Git).
- Animations sauvegardées dans `versions/animation-v3` et `v5`.

## Espace documents — génération de documents (Phase 2, FAITE)
- Bouton **« Générer un document »** (formateur/admin, dans un dossier) → modale.
- Modèle **Interactive Worksheet** : en-tête (intitulé, langue, société, noms/tels/mails apprenant+formateur — **préremplis** depuis le dossier) + **notes** (vocabulaire/structure/communication/autre) + **séances répétables** (date/durée, objectifs, liste de mots, structure & grammaire, pronunciation, erreurs à éviter, pour la prochaine fois).
- Brouillon persistant par dossier : `db.worksheets` (1 par dossier, type `interactive`). API `GET|POST /api/worksheet?group=`, `POST /api/worksheet/generate {group, channel}`.
- Flux UI : bouton « Générer » → **modale de choix du modèle** → **modale plein écran** (formulaire) → **un seul bouton « Générer le document »** + choix du **format (PDF ou Word .docx)**.
- Génération serveur : **PDF** via `pdfkit` (`buildWorksheetPdf`), **Word** via `docx` (`buildWorksheetDocx`). Le fichier est **téléchargé directement** sur l'ordi (réponse binaire `attachment`) — **PAS** déposé dans le dossier, pas de choix de canal. Brouillon (header+séances) persistant par dossier (`db.worksheets`).
- Chaque doc généré porte sur **chaque page** : **logo** en en-tête (haut-gauche, `assets/ls-logo.png`) + **pied de page** = numérotation `x / y` + bloc légal à droite (`LEGAL_LINES` dans server.js : Assoc. Loi 1901, siège, RNA, SIRET, QUALIOPI (constante `QUALIOPI_CERT`), N° déclaration).
- 1ʳᵉ modale = 2 onglets : **Nouveau document** / **Historique** (`db.docgens`, `GET /api/worksheet/history`). La génération **ne ferme pas** la modale (on vérifie le fichier puis on ferme).
- **Historique GÉNÉRIQUE** : helper serveur **`recordDocgen(g, user, {kind, tpl, title, format, apprenant, snapshot})`** appelé par **TOUS** les générateurs (worksheet, test, attestation, contrat, form, leveltest, presence). ⚠️ **Tout nouveau document à générer DOIT appeler `recordDocgen`** sinon il n'apparaît pas dans l'historique. L'entrée stocke `kind` (icône) + `tpl` (clé picker pour ré-ouvrir). Côté client, l'onglet Historique liste tous les types (icône `HIST_IC` + titre) ; clic = `reopenFromHistory(x)` (worksheet → duplique depuis `snapshot` via `openGenModal` ; autres → ré-ouvre le bon générateur via `x.tpl`, même routage que le picker).
- **Ajout de contact** : bouton 🔍 → **modale de recherche** (prénom/nom/email, **insensible aux accents** via `norm()`), exclut les personnes **déjà** dans un dossier avec soi.
- **Questionnaires (QS) en 2 temps** (`db.qs`, `QS_TEMPLATES` mi-parcours `qs_mid` + fin `qs_end`) : le formateur choisit le modèle → remplit l'en-tête (apprenant/société/langue/intitulé/formateur/date) → **« Envoyer à l'apprenant »** (`POST /api/qs/send`) → message spécial `kind:'qs'` dans le **chat commun** + notif. L'apprenant clique « Remplir » → modale plein écran (questions radio/échelle 1-10/texte) → bouton **« Envoyer votre réponse »** → **popup de confirmation** (`confirmDialog`, 2 boutons : « Confirmer l'envoi » / « Continuer à répondre » ; une fois envoyé il ne peut plus modifier) → `POST /api/qs/:id/submit` (**format PDF forcé**, l'apprenant ne choisit pas) génère le doc (mêmes header/footer) et le **dépose dans le canal commun** (statut « Rempli ✓ » + lien dans le chat). Items typés : `intro|section|radio|scale|text` (+ `comment`). `GET /api/qs/:id` renvoie items+réponses.
- **Tests mi-parcours / fin** (`TEST_TEMPLATES` `test_mid` « Test de mi-parcours de formation » + `test_end` « Test de fin de formation ») : générés **par le formateur/admin** (pas de flux apprenant). Modale plein écran = en-tête (apprenant/société/langue/intitulé/formateur/date, préremplis depuis le dossier) + **Résultat** + **Appréciation formateur** + **zone libre enrichie** + format PDF/Word → `POST /api/testdoc/generate {group,type,header,extra,format}` renvoie le **binaire téléchargé directement** (mêmes header/footer logo+légal, PDF=`buildTestPdf`/Word=`buildTestDocx`). Pas de dépôt dans le dossier, la modale reste ouverte (« Document généré ✓ »).
- **Zone libre enrichie (WYSIWYG)** sous l'appréciation des tests : éditeur `contenteditable` (`richEditorHTML`/`wireRichEditor`) avec gras/italique/souligné/**couleur** (noir par défaut)/listes à puces·numérotées/**tableaux** (modale `tableSizeDialog`, pas de `prompt`)/**QCM** (modale `mcqDialog` : question + réponses, clic = bonne réponse cochée, widget `.rt-qcm` contenteditable=false). Sérialisée côté client en blocs (`serializeRich` walk récursif → `[{type:'p',runs:[{text,bold,italic,underline,color}]}|{type:'ul'|'ol',items}|{type:'table',rows}|{type:'qcm',question,options,answer}]`), rendue **sans titre** sur le doc par `richToDocx`/`richToPdf` (server, QCM = question gras + options `(X)`/`( )`), après l'appréciation.
- **Tous les modèles sont rendus en TABLEAUX/cases** (fidèles aux Word source). Helpers partagés : `dxCell`/`dxTable`/`dxRowMin` (Word) et `pdfCell`/`pdfRows` (PDF, dessin manuel + sauts de page). **Interactive Worksheet** (`buildWorksheetPdf`/`buildWorksheetDocx`) = bandeau titre + tableau en-tête (apprenant/formateur label·valeur sur 2 colonnes + notes vocab/structure/comm/autre) + un tableau par **séance**. **Tests mi/fin** (`buildTestPdf`/`buildTestDocx`) = un tableau (titre + en-tête 2 colonnes + lignes **Résultat** et **Appréciation formateur** encadrées). `ws*`/`testdoc/generate` appellent ces builders.
- **Rendu en TABLEAUX (comme les Word d'origine)** : `buildQsPdf`/`buildQsDocx` regroupent (`qsBlocks`/`sameOpts`) les questions radio consécutives à options identiques en **matrices** (critères en lignes × options en colonnes, case **cochée** = fond accent + ✗/X). Les questions `scale` → tableau **1‑10** (case sélectionnée surlignée), `text` → **encadré** de réponse, `intro`/`section` → paragraphes. PDF = dessin manuel de cellules (rect + bordures, saut de page avec ré-affichage de l'en-tête) ; Word = vrais `Table`/`TableRow`/`TableCell` (shading `ShadingType.CLEAR`, en-tête répété `tableHeader:true`). Concerne qs_mid/qs_end/qs_formateur. (Les tests `test_*` restent en lignes label:valeur via `buildSimpleDoc*`.)
- **Fiche satisfaction formateur** (`FORM_TEMPLATES.qs_formateur` « Fiche satisfaction formateur — bilan de formation ») : questionnaire **rempli PAR le formateur lui-même** (pas l'apprenant), téléchargé directement (à transmettre ensuite à l'admin via le **canal privé**). En-tête propre (Formateur/Langue/Nom apprenant/Intitulé/Date, sans Société) + matrice radio 4 niveaux `SC4F` (Oui tout à fait / Partiellement / Pas vraiment / Non, pas du tout) + question ouverte. `GET /api/form/:type` sert le modèle ; `POST /api/form/generate {group,type,header,answers,format}` (canEditWs) renvoie le **binaire** (réutilise `buildQsPdf`/`buildQsDocx`, désormais génériques via `tpl.headerFields || QS_HEADER_FIELDS`). Modale plein écran (en-tête éditable préremplie + items via `qsItemsHTML`), download direct, modale reste ouverte.
- **Attestation de fin de stage** (`buildAttestationPdf`/`buildAttestationDocx`, `POST /api/attestation/generate`, **formateur + admin**, download direct) : en-tête prérempli depuis les fiches (apprenant/société/intitulé/**dates début→fin**/durée/formateur) + objectifs (1/ligne) + **matrice d'évaluation des acquis** (6 compétences × Acquis / En cours d'acquisition / Non acquis, case ✗) + niveau/certification/résultat + commentaires + signatures. Modale `openAttestationModal`.
- **Signature d'Antonin — DEUX variantes** (28/07/2026) : **`assets/signature-antonin.png`** = signature **SEULE** (203×147, fond transparent) et **`assets/signature-antonin-tampon.png`** = signature **sur le tampon L&S** (453×263). Helpers `dxSignatureAntonin` / `pdfSignatureAntonin` / `dxSignatureCell` ; `imgSiPresent` cherche **`data/` d'abord, puis `assets/`** (l'image est livrée avec le site, remplaçable sur un serveur sans toucher au code). **Attestation de fin de stage** → variante **avec tampon**, sous « Antonin HATTABE / Président ». **Feuilles marquées `signAdmin: true`** (Suivi assiduité e-learning + Feuille de présence Test) → variante **SEULE, sans tampon**, incrustée dans la case « Signature administratif » (récapitulatif) ou dans la colonne « Sign administratif » de chaque séance remplie (grille). Fichiers **facultatifs** : absents, rien n'est dessiné et aucune génération n'échoue. `pdfPlacePourSignature` ajoute une page si la place manque. ⚠️ Le dépôt GitHub étant **public**, ces images sont publiquement téléchargeables — choix assumé de l'utilisateur (27/07/2026).
- **Numéro de certificat QUALIOPI** centralisé dans `QUALIOPI_CERT` (= `CERT_S0226_0162` depuis le 27/07/2026) : utilisé par `LEGAL_LINES` (pied de page de tous les documents), l'attestation et le contrat.
- **Contrat de sous-traitance** (`buildContratPdf`/`buildContratDocx` via `contratBlocks(d)`, `POST /api/contrat/generate`, **admin UNIQUEMENT**, download direct) : **mise en forme conforme au document « Modifications Contrat sous-traitance » (27/07/2026)** — les trois termes **Donneur d'ordre**, **Sous-traitant** et **Languages and Success** sont mis en **gras partout** automatiquement (`ctSeg` découpe chaque paragraphe en segments, il n'y a plus de balisage manuel) ; identité du sous-traitant éclatée en lignes (Demeurant / Inscrit au répertoire / Numéro d'activité) ; « ci-après dénommé » en **gras italique** ; saut de ligne avant/après « Il a été convenu ce qui suit » et **avant chaque article** ; article 1 avec intitulé, langue, stagiaire, volumes horaires (`ctHeuresGras`, format **h00** systématique) et dates **en gras** ; article 6 avec taux horaire et total **en gras à deux décimales** (`ctMontant` → « 1 000,00 € ») et le reste **en texte normal** ; zone de date et de signature **nettement dégagée** (2 lignes vides) ; blocs de signature **aux deux extrémités de la même ligne** (« Pour le Donneur d'ordre, Languages and Success / Antonin HATTABE / Président » à gauche, « Pour le Sous-traitant / nom » à droite). texte légal complet **version conforme du Word « Contrat sous traitance modèle » (10 articles, verbatim)** — nouvel **article 2 « Périmètre »** (seules les heures **synchrones** sont sous-traitées ; Elearning/certification/gestion restent au Donneur d'ordre, conformité CPF), article 4 enrichi (liste de documents en sous-tirets `li2`, lois influenceurs/fraude CPF, attestations Urssaf), article 5 donneur d'ordre (code conso, L. 6323-9-1), **article 6 financier** (« par heure de cours **synchrone** » + « (soit XXh00 synchrones) ») dont **tous les AJOUTS du nouveau modèle sont rendus EN GRAS** sur le PDF et le Word (blocs `rp` = segments riches `{t,b}` mélangeant normal/gras dans un même paragraphe ; docx = multi-`TextRun`, pdf = `doc.text(...,{continued})` avec bascule Helvetica/Helvetica-Bold), résiliation 9.1/9.2, litiges art. 10 ; **introduction + article 1 préremplis** depuis les fiches (sous-traitant = formateur : naissance/nationalité/adresse/SIRET/NDA ; objet = intitulé/langue/stagiaire/**programme global (pour information)**/**mission confiée (heures synchrones)**/dates/lieu) ; **article 6** = l'admin saisit taux horaire HT/heure synchrone + montant total HT + volume d'heures synchrones. Modale `openContratModal` (entrée picker visible admin seul ; textareas programme+mission préremplies). Représentant L&S = « Antonin HATTABE » fixe (pas de champ). **Référence générée serveur** `newContratRef()` = `Réf. n° {année}/L&S` + **5 chiffres aléatoires uniques** (jamais réutilisés, stockés dans `db.contratRefs`).
- **Level Test** (`LEVEL_TEST` « Évaluation orale / Questionnaire d'objectifs », `buildLevelTestPdf`/`buildLevelTestDocx`, `GET /api/leveltest` + `POST /api/leveltest/generate {group,fields,format}`, **formateur + admin** `canEditWs`, download direct) : fiche d'**intake** remplie par le formateur. En-tête prérempli depuis la fiche (date du jour, langue, nom/prénom apprenant, mail, société, tél) + **champs personnalisés ajoutables** (`fields.extraHeader = [{label,value}]`, bouton « + Ajouter un champ » dans la modale → lignes `.lt-xf`, rendues en fin d'en-tête sur le doc) + objectifs/profil (textareas) + **Besoins** = un **seul tableau 3 colonnes** `Catégorie | Question | Réponse` avec la **catégorie fusionnée verticalement** à gauche (`pdfBesoins` côté PDF, `dxCell` + `VerticalMergeType` côté Word — fidèle au Word d'origine, distinct des évaluations en 2 colonnes) ; sous-catégories Expression orale, Compréhension orale, Expression écrite, Compréhension écrite, Centres d'intérêt (item au label vide → réponse pleine largeur). **Évaluation écrite** et **Évaluation orale** = titre seul + champs éditables **Type de test** (prérempli Cambridge / TOEIC), **Date évaluation**, **Résultat** (barème libre), **Level / Niveau**. Rendu en tableaux (mêmes header/footer logo+légal). Modale `openLevelTestModal` (sous-titres `.lt-cat`).
- **Corrections du 29/07/2026** : **feuilles administratives** = la signature d'Antonin est **bornée en hauteur** (`pdfSignatureAntonin(…, hMax)` et `dxSignatureCell(largeur, hMax)`) pour ne plus déborder de sa case et mordre sur la ligne suivante ; **questionnaires** (mi-parcours, fin, fiche satisfaction formateur) = interlignes élargis après **chaque** élément, en PDF comme en Word ; **page d'accueil et à-propos** = la **définition de chaque indicateur est rattachée à SON chiffre** (classe `.d`) au lieu d'une note commune, la note ne portant plus que « * Chiffres arrêtés au 31/12/2025. » ; **pages légales** (CGV, confidentialité, règlement intérieur, mentions légales) = mention « Document créé le 01/06/2026 · » devant la date de mise à jour. ⚠️ i18n : la note ayant changé de texte, sa clé a dû être remplacée dans les 5 dictionnaires et les deux définitions y ont été ajoutées.
- **Kit formateur — corrections du 28/07/2026** : **attestation** = 2 interlignes avant chaque titre, ligne « Niveau atteint à l'issue de la formation » **en gras** ; **Suivi assiduité e-learning** et **Feuille de présence Test** (drapeau `signAdmin`) = champ d'en-tête « Administratif » **prérempli à Antonin HATTABE**, libellé « Signature Formateur » → **« Signature administratif »** (colonne « Sign administratif » sur la grille), **signature d'Antonin SANS tampon** incrustée en face, et **suppression du bloc de bas de page** « Pour Languages & Success / Antonin HATTABE, Président ». ⚠️ Conséquence : sur ces deux feuilles le **formateur ne signe plus** — le pad est masqué dans la modale et `POST /api/presence/send` n'exige plus `formateurSig`. **Questionnaires mi-parcours et fin** : champ **Certification retiré** du formulaire d'envoi.
- **Kit formateur — corrections du 27/07/2026** : **attestation de fin de stage** = titres aérés, « Compétence**s** » au pluriel, ligne « Niveau atteint » dégagée au-dessus et en dessous, **certification en gras** (TOEIC, Bright Language…), bloc de signature sur **une ligne horizontale complète** (Antonin HATTABE / Président · Le Formateur · L'apprenant) ; **feuille e-learning** renommée **« Suivi assiduité »** (titre du document et libellé du modèle) ; e-learning et **feuille Test (certification)** : libellé « Formateur » → **« Administratif »** + signature du président incrustée ; **tests mi-parcours et fin** : la ligne **Certification est supprimée** (document ET formulaire).
- **Feuille de présence** (`PRESENCE_TEMPLATES` 3 types, `buildPresencePdf`/`buildPresenceDocx` + `pdfPresenceGrid`, **formateur + admin** `canEditWs`) : modale `openPresenceModal` avec **sélecteur de type** (re-rend le formulaire) → **e-learning** (`kind:'summary'` : en-tête + Nbre d'heures prévues/connexion réalisées + Date du rapport + bloc **Signature Formateur / Signature Apprenant**) · **présentiel-distanciel** et **test** (`kind:'grid'` : en-tête + **grille** créneaux 0:30→10:00 + colonnes Date/Jour/H début/H fin/Durée/**Sign Formateur**/**Sign Apprenant**). Chaque séance répétable `.pr-sess` a un **select « Créneau »** (`s.slot`, options 0:30→10:00, défaut = `nextSlot()` 1ᵉʳ libre) : le serveur place la séance sur **la ligne du créneau choisi** (`slotMap[time]`) et **coche la case** correspondante (PDF = carré rempli accent, Word = ✗ + fond). En-tête prérempli depuis la fiche ; **Lieu** = select Présentiel/Distanciel. (Tous les `<select>` de formulaire sont stylés comme les inputs via `.gf select` / `.pr-sess select` : `appearance:none` + flèche SVG accent.) Titre du doc = « FEUILLES DE PRÉSENCE » (mêmes header/footer logo+légal). Modèles tirés des Word/PDF source ELSYS.
  - **Flux de signatures (comme le QS)** : le formateur **remplit + signe** (pad réutilisable `mountSignaturePad`/`sigPadHTML` : dessin souris/tactile **ou** téléversement d'image → data URL PNG) → **« Envoyer à l'apprenant pour signature »** (`POST /api/presence/send {group,type,fields,formateurSig}`) → message spécial `kind:'presence'` dans le **chat commun** + notif (`db.presences`). L'apprenant clique **« Signer »** → modale `openPresenceSignModal` (même pad) → **« Envoyer ma signature »** (`POST /api/presence/:id/sign {sig}`) → génère le **PDF final avec les 2 signatures incrustées** (`sigImg` data URL→buffer ; pdfkit `doc.image` / docx `ImageRun` ; grille = sig répétée sur chaque séance remplie, e-learning = bloc bas), le **dépose dans le canal commun** (statut « Signée ✓ » + lien) et notifie le formateur. `GET /api/presence/:id` = statut. `express.json({limit:'2mb'})` pour absorber les data URLs. (`POST /api/presence/generate` direct PDF/Word reste dispo mais n'est plus appelé par l'UI.)
- **Numérotation des documents générés** (préfixe du nom de fichier + ordre du picker `renderTplPane`), **dépendante du rôle** pour éviter le « trou » du Contrat (admin only) : **1** Interactive Worksheet · **2** QS mi-parcours · **3** QS fin de formation · **4** Attestation de fin de formation · **5** Test mi-parcours · **6** Test fin de formation, puis — **admin** : **7** Contrat de sous-traitance · **8** QS Formateur · **9** Level Test · **10** Feuille de présence ; **formateur** (pas de contrat) : **7** QS Formateur · **8** Level Test · **9** Feuille de présence. (Côté client `ME.role === 'admin' ? …` dans le picker + l'`a.download` ; côté serveur `req.user.role === 'admin' ? …` dans le `Content-Disposition`.)

## Espace documents — suite
- Autres modèles de documents à ajouter au générateur (l'utilisateur fournira les formats).
- (Phase 3 « Agenda + rappels » : **abandonnée pour l'instant** à la demande de l'utilisateur.)

## Pistes non faites
Déploiement en ligne (Render/Railway) ; brancher les formulaires contact/test au backend ; suppression doc/contact ; relecture native des questions du test.

### Conformité / pages légales (audit du 30/07/2026)
- **`mentions-legales.html` — document « Mentions légales 2 » appliqué le 30/07/2026** : date, nouvelle section **Certification qualité** (certificat `CERT_S0226_0162`, CEVA SOLUTION, Cofrac 5-0667, valable jusqu'au 05/01/2028 + lien `assets/Attestation-Qualiopi.pdf`), **hébergeur = AMAZON WEB SERVICES EMEA SARL, succursale française** (Courbevoie) au lieu d'AWS Inc. Seattle, marques de tiers (Qualiopi), bases légales + durées de conservation, droits RGPD élargis (limitation/portabilité/retrait), adresse CNIL, nouvelle section **Conditions générales, tarifs et rétractation** → CGV. Titres en casse normale (pas les capitales du Word) pour rester cohérents avec la page ; lien PDF **relatif**. ⚠️ **Cloudflare, Inc. n'est PAS nommé** alors que tout le trafic transite par le tunnel (`cloudflared` dans `docker-compose.yml`, `Server: cloudflare` en prod) : ajout laissé à la décision de l'utilisateur. ⚠️ Le code prouve le **mécanisme** (runner `self-hosted` + tunnel) mais PAS le fournisseur de la machine — ne pas en déduire l'hébergeur.
- **`confidentialite.html` § 6 réécrit le 30/07/2026** → « 6. Cookies, traceurs et stockage local ». Faits **vérifiés par audit + 3 réfutateurs** : **aucun cookie** (0 `document.cookie`, 0 `Set-Cookie` sur 4 requêtes prod dont un POST `/api/login`, aucune dépendance cookie/session dans `package.json`) ; aucun analytics/pixel/widget social (icônes = simples `<a>`) ; stockage = `ls-lang`, `ls-seen-splash` (**sessionStorage**, pas localStorage), `lsx_token`, `ls-tweaks-*`, tous fonctionnels → exemptés (art. 82 loi 78-17, qui vise **toute lecture ou inscription** dans un terminal, pas seulement les cookies). ⚠️ Formulations volontairement **bornées** après réfutation : « aucune mesure d'audience et aucun profilage » (pas « aucun suivi » : `db.logins` enregistre l'IP), « effacé de votre navigateur dès votre déconnexion » (pas « supprimé » : aucune révocation serveur), « repères techniques d'affichage » (pas « vos préférences mémorisées » : `ls-tweaks-*` est lu partout mais **aucun chemin atteignable ne l'écrit**), et **jamais** de localisation des serveurs de Google (les nœuds qui répondent sont dans l'allocation RIPE — affirmation invérifiable). Ce qui invaliderait le texte : activer une protection anti-bot Cloudflare (poserait `__cf_bm`), ou ajouter un hôte tiers.
- **Google Fonts : SUPPRIMÉ le 30/07/2026 — polices auto-hébergées.** C'était le seul transfert de données vers un tiers (les 13 pages chargeaient `fonts.googleapis.com`/`fonts.gstatic.com` aux lignes 9-10-11, le `preconnect` déclenchant la requête dès l'analyse du `<head>`, sans interaction). Les 3 lignes sont remplacées par **une seule** : `<link rel="stylesheet" href="assets/fonts.css?v=BUILD" />`. `assets/fonts.css` = la feuille css2 de Google régénérée avec des chemins locaux (mêmes familles, axes et graisses → rendu identique), `assets/fonts/` = **9 fichiers .woff2** (600 ko) + `OFL.txt` (la SIL Open Font License **exige** que l'avis accompagne les fichiers). Vérifié : 0 requête tierce, polices réellement appliquées (largeur de texte ≠ fallback Georgia/system-ui). ⚠️ **Régénérer avec un User-Agent moderne**, sinon Google renvoie du TTF au lieu du woff2. ⚠️ Seules **Newsreader** (titres) et **Work Sans** (corps) sont auto-hébergées : `index.html` demandait en plus **Crimson Pro** et **Space Grotesk**, qui ne servent qu'aux variantes du panneau interne `LS_CONFIG.fonts` — inaccessible (il ne s'ouvre que sur un `postMessage '__activate_edit_mode'` qu'aucun fichier n'envoie). Si ce panneau redevient utilisable, il faut auto-héberger ces 2 familles ou ses variantes retomberont sur Georgia/system-ui.
- **Autres écarts relevés par l'audit, NON corrigés** : (1) `db.logins` enregistre `{user,email,ip,date}` à chaque connexion mais le § 2 (collecte) et le § 7 (conservation) de `confidentialite.html` ne mentionnent ni l'IP ni ce journal, borné au **nombre** (1000) et non au temps ; (2) le **JWT circule en clair dans les URL** de téléchargement (`account.js` ~303/313/370/755/759, car un `<a>` ne peut pas porter d'en-tête `Authorization`) → il entre dans l'historique du poste ; la déconnexion n'efface que la copie locale, le jeton reste valide **30 jours** ; (3) les § 2, 3 et 4 restreignent tout le propos au « formulaire de contact » alors que le site héberge un espace documents à comptes nominatifs ; (4) `i18n.js` bascule `<html lang>` alors que le corps des pages légales reste français.
- Détail : `index.html` déclare un importmap **three.js depuis jsdelivr** que plus rien n'importe (code mort, aucune requête déclenchée) — supprimable, et c'est le seul moyen par lequel « seule connexion à un service tiers » pourrait devenir faux par inadvertance.
